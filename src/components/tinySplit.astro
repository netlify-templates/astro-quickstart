---
import Split from '../components/split.astro'

export interface Props {
	idName: string;
  classItems: string;
}

const { idName, classItems } = Astro.props as Props;
let className = 'tiny'
if (!!classItems) { 
  className = `${className} ${classItems}`
}

---
<Split classItems={className} id={idName}>
  <slot />
</Split>